inits.push(function(param) {
  if(!param.qstopic || 1) return;
//  insertAnchor = function(el) {
//    location.replace(el.innerHTML);
//    var odkaz = "http://djpw.cz/" + param.qstopic + (parseInt(param.qspage) > 0 ? "/" + (parseInt(param.qspage) + 1) : "") + el.innerHTML;
//    odkaz = "[url=" + odkaz + "]" + el.innerHTML + "[/url]";

//    insertName(el, odkaz);
//    // chyba neodscrollovania na textareu po volaní insertName()
//    var t = document.forms['postMsg'] && document.forms['postMsg'].elements['poslText'];
//    if(!t) {
//      console && console.log("Nie je monžné aktivovať textareu (vloženie kotvy insertAnchor()).");
//      return;
//    }
//    //t.focus();
//    t.blur();
//    //t.scrollIntoView && t.scrollIntoView();
//    //t.focus();
//  }
});
